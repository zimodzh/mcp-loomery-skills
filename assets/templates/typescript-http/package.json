{
  "name": "{{PACKAGE_NAME}}",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "inspector": "npm run build && npx @modelcontextprotocol/inspector --server-url http://127.0.0.1:3000/mcp --transport http"
  },
  "dependencies": {
    "@modelcontextprotocol/node": "^2",
    "@modelcontextprotocol/server": "^2",
    "zod": "^4"
  },
  "devDependencies": {
    "@types/node": "^24",
    "typescript": "^5"
  }
}
