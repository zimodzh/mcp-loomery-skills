# {{SERVER_NAME}}

<p align="center">
  <samp>
    <a href="./README.md">中文</a> ·
    <strong>English</strong>
  </samp>
</p>

Local MCP server generated by mcp-loomery.

## Run

TypeScript:

```bash
npm install
npm run inspector
```

Python:

```bash
uv sync
uv run mcp dev server.py
```

stdio logs go to **stderr** only. Do not `console.log` / `print` on stdout.
